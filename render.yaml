services:
  - type: web
    name: pptx-translator
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -k uvicorn.workers.UvicornWorker --timeout 300 --keep-alive 300 main:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
